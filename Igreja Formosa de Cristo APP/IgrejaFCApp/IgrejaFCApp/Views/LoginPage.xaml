<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding Title}"
    x:Class="IgrejaFCApp.Views.LoginPage"
    BackgroundImage="fundo.png">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <ResourceDictionary>
                <Style TargetType="Entry">
                    <Setter Property="Margin" Value="40,10" />
                </Style>
            </ResourceDictionary>
        </Grid.Resources>

        <StackLayout Grid.Row="1" VerticalOptions="FillAndExpand">
            <Entry Text="{Binding UserName}" Placeholder="Digite o CPF" VerticalOptions="EndAndExpand" />
            <Entry Text="{Binding Password}" IsPassword="true" Placeholder="Digite a senha" VerticalOptions="StartAndExpand" />
            <Button Text="Entrar" HorizontalOptions="Center" VerticalOptions="EndAndExpand"  Command="{Binding LoginCommand}"  BackgroundColor="White"/>
            
        </StackLayout>

        

    </Grid>
    
</ContentPage>
